/*$(window).load(function() {
	//alert("loaded!");
	jQuery.ajax({
		url: url,
		type: 'post',
		data: {
			uuid: uuid,
			session_id: sessionID
		}
		success: display_result
	});
});*/

function display_results(ajax) {
	
}